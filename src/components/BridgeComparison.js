export default function BridgeComparison({ bridges, visibleBridges }) { return ( <div className="border rounded p-4 mb-4"> <h3 className="text-lg font-semibold mb-3">Bridge Comparison</h3> <div className="space-y-3"> {bridges.map(bridge => ( <div key={bridge.id} className={`${!visibleBridges[bridge.id] ? "opacity-50" : ""}`}> <div className="flex justify-between items-center mb-1"> <div className="flex items-center"> <span className="inline-block w-3 h-3 rounded-full mr-2" style={{ backgroundColor: bridge.color }}></span> <span className="font-medium">{bridge.name}</span> </div> <span className="font-semibold">{bridge.value}</span> </div> <div className="w-full bg-gray-200 rounded-full h-2.5"> <div className="bg-blue-600 h-2.5 rounded-full" style={{ width: bridge.percentage, backgroundColor: bridge.color }}></div> </div> </div> ))} </div> </div> ); }

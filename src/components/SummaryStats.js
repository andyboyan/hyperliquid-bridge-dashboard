export default function SummaryStats({ stats }) { return ( <div className="grid grid-cols-2 md:grid-cols-4 gap-4 mb-6"> {stats.map(stat => ( <div key={stat.id} className="border rounded shadow-sm p-4"> <h3 className="text-sm text-gray-500">{stat.label}</h3> <p className="text-2xl font-bold">{stat.value}</p> {stat.change && ( <p className={`text-sm ${stat.change.startsWith("+") ? "text-green-600" : "text-red-600"}`}> {stat.change} from previous period </p> )} </div> ))} </div> ); }

export default function FilterButtons({ title, options, activeOptions, onToggle }) { return ( <div className="mb-4"> <p className="font-medium mb-2">{title}:</p> <div className="flex flex-wrap gap-2"> {options.map(option => ( <button key={option.id} onClick={() => onToggle(option.id)} className={`px-3 py-1 rounded-full text-sm ${activeOptions[option.id] ? "bg-gray-800 text-white" : "bg-gray-200 text-gray-800"}`}> {option.label} </button> ))} </div> </div> ); }
